import React from "react";
import LoginBtn from "./LoginBtn";

export default props => {
    return (
        <div className="login-wrap">
            <LoginBtn class="login-btn" />
        </div>
    );
};